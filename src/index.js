import React from 'react';
import ReactDOM from 'react-dom/client';
import MonComposant from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MonComposant />
  </React.StrictMode>
);


